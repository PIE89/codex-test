# План разработки task-трекера

Будем делать базовый task-трекер на React + TypeScript. Задачи хранятся в `localStorage`, поэтому список и отметки важности сохраняются после обновления страницы.

## Что должно получиться

Приложение состоит из двух основных частей.

### Форма добавления задачи

- управляемое текстовое поле;
- максимальная длина введённого значения — 80 символов;
- справа от поля находится кнопка «Добавить»;
- кнопка имеет состояние `disabled`, пока поле пустое или содержит только пробельные символы;
- задача добавляется отправкой формы — кнопкой «Добавить» или клавишей Enter;
- перед сохранением пробелы в начале и конце удаляются;
- после успешного добавления поле очищается, а кнопка снова становится недоступной.

### Динамический нумерованный список

- новая задача добавляется в конец списка;
- у каждой задачи есть чекбокс выполнения;
- возле каждой задачи находятся кнопки «Важно» и «Удалить»;
- выполненная задача получает зелёное оформление и курсивный текст без перечёркивания;
- кнопка «Удалить» удаляет конкретную задачу;
- кнопка «Важно» переключает состояние и оформление конкретной задачи;
- после удаления нумерация автоматически перестраивается.

### Сохранение между сессиями

- при запуске приложение загружает задачи из `localStorage`;
- добавление, изменение важности, изменение выполнения и удаление автоматически сохраняют новый список;
- после обновления страницы сохраняются порядок, текст, статус важности и статус выполнения каждой задачи;
- некорректные данные в хранилище не должны ломать приложение.

## Подробный план

### 1. Подготовить модель задачи

Описать TypeScript-тип `Task` со следующими полями:

- `id` — уникальный идентификатор;
- `text` — текст задачи длиной не более 80 символов;
- `isImportant` — признак важной задачи;
- `isCompleted` — признак выполненной задачи.

```ts
type Task = {
  id: string
  text: string
  isImportant: boolean
  isCompleted: boolean
}
```

Уникальный идентификатор позволит изменять и удалять конкретную задачу, даже если несколько задач имеют одинаковый текст.

### 2. Определить состояние приложения

В главном компоненте предусмотреть два состояния:

- текущее значение текстового поля;
- массив добавленных задач.

```ts
const [taskText, setTaskText] = useState('')
const [tasks, setTasks] = useState<Task[]>([])
```

При изменении поля обновляется `taskText`. При добавлении, удалении или изменении важности обновляется `tasks`.

### 3. Создать форму добавления

Разместить в элементе `<form>`:

- `<input>` для текста задачи;
- `<button type="submit">Добавить</button>` справа от поля.

Поле сделать управляемым:

- `value` получает значение из `taskText`;
- `onChange` обновляет `taskText`;
- `maxLength={80}` не позволяет ввести более 80 символов.

Кнопку расположить справа с помощью Flexbox или Grid. Форму обрабатывать через `onSubmit`, чтобы она работала одинаково при нажатии кнопки и клавиши Enter.

### 4. Реализовать проверку введённого текста

Для проверки использовать очищенное значение:

```ts
const normalizedTaskText = taskText.trim()
const isTaskValid =
  normalizedTaskText.length > 0 && normalizedTaskText.length <= 80
```

Кнопка «Добавить» должна получать:

```tsx
disabled={!isTaskValid}
```

Добавить нельзя:

- пустую строку;
- один или несколько пробелов;
- строку только из пробельных символов и переносов;
- текст длиннее 80 символов.

Ограничение должно присутствовать и в интерфейсе через `maxLength`, и в обработчике отправки. Повторная проверка в обработчике защищает логику приложения независимо от состояния HTML-поля.

Строку вида `"  Купить молоко  "` можно добавить, но перед сохранением внешние пробелы нужно удалить. Под символом понимается любой непробельный символ: буква, цифра или знак.

### 5. Реализовать добавление задачи

При отправке формы:

1. Вызвать `event.preventDefault()`.
2. Получить значение через `trim()`.
3. Повторно проверить, что длина результата находится в диапазоне от 1 до 80 символов.
4. Создать задачу с уникальным `id` и `isImportant: false`.
5. Добавить задачу в конец массива.
6. Очистить текстовое поле через `setTaskText('')`.

После очистки кнопка «Добавить» автоматически снова станет недоступной.

### 6. Создать нумерованный список

Для семантически правильной нумерации использовать `<ol>` и `<li>`. Массив задач преобразовывать в элементы списка через `tasks.map()`.

В качестве React-ключа использовать `task.id`, а не индекс массива. Тогда удаление элемента не нарушит соответствие задач и элементов интерфейса.

### 7. Описать вид одной задачи

Каждый пункт списка должен содержать:

- номер, создаваемый элементом `<ol>`;
- текст задачи;
- кнопку «Важно»;
- кнопку «Удалить».

При необходимости пункт можно вынести в компонент `TaskItem`:

```ts
type TaskItemProps = {
  task: Task
  onToggleImportant: (id: string) => void
  onToggleCompleted: (id: string) => void
  onDelete: (id: string) => void
}
```

Для первой учебной версии допустимо оставить всю логику в `App.tsx`, а разделить её на компоненты после появления рабочей версии.

### 8. Реализовать удаление

Кнопка «Удалить» передаёт `id` выбранной задачи. Из массива исключается элемент с этим идентификатором:

```ts
tasks.filter((task) => task.id !== id)
```

React перерисует список, а `<ol>` автоматически пересчитает номера оставшихся задач.

### 9. Реализовать кнопку «Важно»

По нажатию найти задачу по `id` и переключить значение:

```ts
isImportant: !task.isImportant
```

Первое нажатие отмечает задачу как важную, повторное снимает отметку. Важную задачу выделить фоном, рамкой, начертанием текста или цветом кнопки.

Кнопке добавить атрибут:

```tsx
aria-pressed={task.isImportant}
```

Перемещать важные задачи наверх на этом этапе не требуется.

### 10. Добавить состояние пустого списка

Пока задач нет, вместо пустого списка вывести сообщение «Задач пока нет». После добавления первой задачи сообщение исчезает и появляется список.

### 11. Оформить интерфейс

Предусмотреть стили для:

- общего контейнера и заголовка;
- формы, текстового поля и кнопки справа;
- активной кнопки «Добавить» и её состояния `disabled`;
- нумерованного списка и отдельного пункта;
- обычной и важной задачи;
- кнопок «Важно» и «Удалить»;
- состояний наведения и фокуса;
- небольшого экрана.

Заблокированная кнопка должна визуально отличаться от доступной, но текст оставаться читаемым.

### 12. Проверить пограничные случаи

Проверить:

- пустое поле;
- один и несколько пробелов;
- пробельные символы и переносы без текста;
- ввод 1 символа;
- ввод ровно 80 символов;
- невозможность ввести 81-й символ;
- повторную защитную проверку длины при отправке формы;
- текст с пробелами по краям;
- добавление кнопкой и клавишей Enter;
- очистку поля после успешного добавления;
- последовательное добавление нескольких задач;
- одинаковый текст у нескольких задач;
- удаление первой, средней, последней и единственной задачи;
- переключение важности несколько раз;
- правильную нумерацию после удаления.

### 13. Провести техническую проверку

После реализации выполнить:

```powershell
npm run lint
npm run build
```

Затем запустить приложение:

```powershell
npm run dev
```

Проверить поведение вручную в браузере и убедиться, что в консоли нет ошибок.

### 14. Сохранить результат в Git

Перед фиксацией результата проверить состав изменений:

```powershell
git status
git diff
```

После проверки создать коммит. Отправку в удалённый репозиторий выполнять отдельно, когда это потребуется.

### 15. Добавить сохранение в localStorage

Использовать отдельный ключ приложения. При инициализации состояния безопасно прочитать сохранённое значение, разобрать JSON и проверить структуру каждой задачи.

После любого изменения массива задач сохранять его актуальное состояние. Это автоматически охватывает:

- добавление задачи;
- переключение статуса «Важно»;
- удаление задачи.

Ошибки чтения, некорректный JSON или недоступность хранилища не должны приводить к падению интерфейса.

### 16. Добавить отметку выполнения задачи

Расширить тип `Task` полем `isCompleted`. Новая задача создаётся со значением `isCompleted: false`.

Для каждого пункта списка добавить доступный чекбокс:

- чекбокс переключает только выбранную задачу по её `id`;
- установленная галочка означает, что задача выполнена;
- повторное нажатие снимает отметку выполнения;
- изменение происходит через обновление React-состояния без перезагрузки страницы;
- чекбокс должен иметь понятное доступное название с текстом соответствующей задачи.

Выполненную задачу оформить следующим образом:

- использовать зелёный фон, рамку или цветовой акцент всей карточки;
- текст задачи сделать курсивным;
- не использовать перечёркивание, чтобы текст оставался легко читаемым;
- визуально различать установленный и снятый чекбокс;
- плавно и симметрично анимировать переход в выполненное состояние и возврат к нейтральному или важному оформлению без бликов;
- заранее отрисовывать обычное и курсивное начертания и переключать их через синхронное перекрёстное проявление без задержки шрифта;
- предусмотреть состояния наведения и фокуса.

Статус `isCompleted` сохранять в `localStorage` вместе с остальными полями. После обновления страницы галочка и зелёное оформление должны восстанавливаться.

Принятые решения по оформлению:

- существующий круг работает как чекбокс: активная задача показывает номер, выполненная — галочку;
- зелёное оформление выполнения имеет приоритет над красным, но статус важности сохраняется и снова отображается после снятия галочки.

## Этапы реализации

### Этап 1: форма — завершён

На текущем этапе реализовать:

- управляемое поле с ограничением 80 символов;
- кнопку «Добавить» справа;
- `disabled` для пустого значения и одних пробелов;
- отправку формы кнопкой и клавишей Enter;
- очистку поля после успешной отправки.

### Этап 2: список и действия — завершён

После завершения и проверки формы добавить модель задачи, массив задач, нумерованный список, отметку важности и удаление.

### Этап 3: сохранение — завершён

Подключить `localStorage` и проверить восстановление списка и статуса важности после обновления страницы.

### Этап 4: выполнение задачи — завершён

Добавить чекбокс выполнения, зелёное оформление, курсивный текст и сохранение статуса `isCompleted` в `localStorage`.

### Этап 5: фильтрация по названию — завершён

Добавить над списком отдельную управляемую форму поиска:

- фильтровать задачи сразу при вводе, без отправки формы и перезагрузки страницы;
- искать по частичному совпадению без учёта регистра;
- игнорировать пробелы по краям поискового запроса;
- при пустом запросе показывать полный список;
- показывать количество найденных задач относительно общего количества;
- предусмотреть доступную кнопку очистки запроса;
- при отсутствии совпадений показывать отдельное сообщение и возможность сбросить фильтр;
- не сохранять поисковый запрос в `localStorage`;
- все действия над отфильтрованными задачами выполнять с исходным массивом по `id`.

### Этап 6: редактирование названия — завершён

Добавить inline-редактирование непосредственно в карточке задачи:

- расположить кнопку «Редактировать» перед кнопкой «Важно»;
- заменять текст задачи на управляемое поле с текущим названием;
- автоматически переводить фокус в поле;
- заменять «Редактировать» на акцентную кнопку «Сохранить» и показывать «Отмена»;
- применять ограничение от 1 до 80 символов после `trim()`;
- отключать сохранение для пустого, пробельного и неизменённого значения;
- сохранять по кнопке и Enter, отменять по кнопке и Escape;
- обновлять задачу иммутабельно по `id`, сохраняя статусы важности и выполнения;
- разрешать редактирование только одной задачи одновременно;
- сохранять новое название в `localStorage` существующим механизмом;
- при активном фильтре после сохранения повторно применять его к новому названию.

### Этап 7: фильтрация по статусу — завершён

Добавить над поиском доступную группу из трёх взаимоисключающих переключателей:

- варианты «Все», «Важные» и «Выполненные» реализовать радиокнопками в виде крупных карточек;
- активный вариант выделять кислотно-зелёной заливкой;
- показывать на каждой карточке независимое количество задач;
- в «Важные» включать все задачи с `isImportant`, в том числе выполненные;
- в «Выполненные» включать все задачи с `isCompleted`, в том числе важные;
- объединять статусный фильтр с поиском по названию;
- обновлять итоговый счётчик и список без перезагрузки;
- при отсутствии результата показывать существующее пустое состояние;
- сбрасывать одновременно текстовый и статусный фильтры;
- не сохранять выбранный фильтр в `localStorage`.

### Этап 8: компонентный рефакторинг — завершён

Разделить монолитный `App.tsx` и общий CSS без изменения поведения:

- оставить в `App.tsx` композицию интерфейса и координацию UI-состояния;
- вынести форму добавления в `TaskForm`;
- вынести поиск и статусные переключатели в `TaskFilters`;
- вынести заголовок, пустые состояния и коллекцию задач в `TaskList`;
- вынести карточку задачи, действия и inline-редактирование в `TaskItem`;
- вынести операции над массивом и синхронизацию в `useTasks`;
- вынести типы, константы и безопасную работу с `localStorage` в отдельные модули;
- хранить стили компонентов рядом с соответствующими компонентами;
- сохранить ключ и формат данных `localStorage`, внешний вид, анимации и доступность.

## Критерии готовности всего проекта

Проект можно считать готовым, если:

- пользователь может ввести от 1 до 80 символов;
- в поле невозможно ввести более 80 символов;
- пустую строку или строку из пробелов добавить нельзя;
- кнопка «Добавить» правильно включается и отключается;
- задача добавляется кнопкой и клавишей Enter;
- поле очищается после успешного добавления;
- задачи отображаются в динамическом нумерованном списке;
- каждую задачу можно отметить как важную и снять отметку;
- каждую задачу можно отметить как выполненную и вернуть в активное состояние;
- выполненная задача выделяется зелёным цветом и курсивом без перечёркивания;
- статус выполнения сохраняется после обновления страницы;
- каждую задачу можно удалить;
- после удаления нумерация остаётся правильной;
- в консоли браузера нет ошибок;
- `lint` и `build` проходят успешно.

После реализации фильтрации дополнительно проверить, что поиск работает по полному и частичному названию без учёта регистра, а очистка запроса возвращает полный список.

## Пошаговый чек-лист

### Форма

- [x] Добавить состояние для текста поля.
- [x] Создать форму с управляемым полем ввода.
- [x] Установить полю `maxLength={80}`.
- [x] Добавить кнопку «Добавить» справа от поля.
- [x] Проверять значение через `trim()`.
- [x] Отключать кнопку для пустой строки и пробельных символов.
- [x] Повторно проверять диапазон от 1 до 80 символов в `onSubmit`.
- [x] Добавить поддержку отправки клавишей Enter.
- [x] Очищать поле после успешной отправки.
- [x] Проверить ввод 1, 80 и попытку ввода 81 символа.

### Список задач

- [x] Описать тип `Task`.
- [x] Добавить состояние для массива задач.
- [x] Реализовать добавление задачи в массив.
- [x] Вывести задачи через `<ol>`.
- [x] Использовать уникальный `id` как React-ключ.
- [x] Добавить кнопку «Важно».
- [x] Реализовать переключение важности.
- [x] Добавить визуальное выделение важной задачи.
- [x] Добавить атрибут `aria-pressed`.
- [x] Добавить кнопку «Удалить».
- [x] Реализовать удаление по `id`.
- [x] Проверить автоматическую перенумерацию.
- [x] Добавить сообщение для пустого списка.

### Сохранение в localStorage

- [x] Добавить отдельный ключ хранилища для списка задач.
- [x] Безопасно загружать и проверять сохранённые задачи при запуске.
- [x] Сохранять массив после добавления задачи.
- [x] Сохранять изменение статуса «Важно».
- [x] Сохранять удаление задачи.
- [x] Проверить восстановление текста, порядка и важности после обновления страницы.
- [x] Не допускать падения приложения при ошибке доступа к хранилищу.

### Выполнение задач

- [x] Согласовать расположение чекбокса относительно номера задачи.
- [x] Согласовать приоритет оформления для одновременно важной и выполненной задачи.
- [x] Добавить поле `isCompleted` в тип `Task`.
- [x] Создавать новые задачи со значением `isCompleted: false`.
- [x] Добавить чекбокс выполнения для каждого пункта списка.
- [x] Реализовать переключение выполнения по `id` без перезагрузки.
- [x] Добавить доступное название чекбокса с текстом задачи.
- [x] Оформить выполненную задачу зелёным цветом.
- [x] Сделать текст выполненной задачи курсивным без перечёркивания.
- [x] Убрать блик и добавить симметричный плавный переход при установке и снятии выполнения.
- [x] Устранить запаздывание курсивного начертания через предварительную отрисовку и перекрёстное проявление текста.
- [x] Добавить состояния наведения и фокуса для чекбокса.
- [x] Сохранять `isCompleted` в `localStorage`.
- [x] Проверить восстановление галочки и оформления после обновления страницы.
- [x] Проверить снятие отметки выполнения.

### Фильтрация задач

- [x] Добавить отдельное состояние поискового запроса.
- [x] Добавить доступную форму поиска над списком.
- [x] Реализовать частичное сравнение без учёта регистра.
- [x] Игнорировать пробелы по краям запроса.
- [x] При пустом запросе показывать все задачи.
- [x] Показывать число совпадений и общее число задач.
- [x] Добавить кнопку очистки поискового запроса.
- [x] Показать отдельное состояние при отсутствии совпадений.
- [x] Проверить поиск по полному названию.
- [x] Проверить поиск по части названия и в другом регистре.
- [x] Проверить запрос без совпадений и его очистку.
- [x] Проверить добавление, удаление и изменение статусов при активном фильтре.
- [x] Проверить, что после обновления страницы задачи сохраняются, а фильтр сбрасывается.

### Редактирование задач

- [x] Добавить состояния редактируемой задачи и нового названия.
- [x] Добавить кнопку «Редактировать» перед кнопкой «Важно».
- [x] Заменять название на поле и автоматически переводить в него фокус.
- [x] Добавить кнопки «Сохранить» и «Отмена».
- [x] Отключать сохранение пустого, пробельного и неизменённого названия.
- [x] Ограничить новое название 80 символами.
- [x] Сохранять кнопкой и клавишей Enter.
- [x] Отменять кнопкой и клавишей Escape.
- [x] Проверить переход к редактированию другой задачи.
- [x] Проверить редактирование важной и выполненной задачи без потери статусов.
- [x] Проверить редактирование при активном фильтре.
- [x] Проверить сохранение нового названия после обновления страницы.
- [x] Проверить мобильную компоновку режима редактирования.

### Фильтрация по статусу

- [x] Добавить тип и состояние статусного фильтра.
- [x] Добавить переключатели «Все», «Важные» и «Выполненные» над поиском.
- [x] Обеспечить выбор только одного варианта.
- [x] Добавить независимые динамические счётчики.
- [x] Объединить фильтрацию по статусу с поиском по названию.
- [x] Обновлять список и итоговое количество без перезагрузки.
- [x] Добавить общий сброс текстового и статусного фильтров.
- [x] Проверить важную выполненную задачу в обеих категориях.
- [x] Проверить изменение статуса при активном фильтре.
- [x] Проверить сброс фильтра после обновления страницы.
- [x] Проверить управление с клавиатуры и мобильную компоновку.

### Компонентный рефакторинг

- [x] Вынести типы и общие константы.
- [x] Вынести чтение и запись `localStorage` в `taskStorage`.
- [x] Создать `useTasks` с операциями добавления, обновления, статусов и удаления.
- [x] Создать компоненты `TaskForm`, `TaskFilters`, `TaskList` и `TaskItem`.
- [x] Оставить в `App.tsx` композицию и состояние интерфейса.
- [x] Разнести CSS по папкам компонентов.
- [x] Сохранить управляемое редактирование только одной задачи.
- [x] Проверить все пользовательские сценарии после переноса.
- [x] Сравнить внешний вид на большом и небольшом экране.
- [x] Проверить восстановление существующих данных из `localStorage`.

### Оформление и проверка

- [ ] Оформить активные, заблокированные, наведённые и фокусные состояния.
- [x] Проверить интерфейс на небольшом экране.
- [x] Проверить все пограничные сценарии.
- [x] Выполнить `npm run lint`.
- [x] Выполнить `npm run build`.
- [x] Проверить приложение в браузере.
- [x] Просмотреть изменения через `git diff`.
- [ ] Создать коммит после проверки.
- [ ] При необходимости отправить коммит в удалённый репозиторий.
